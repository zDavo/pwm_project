<h1 class="text-center">Le nostre pizze</h1>
<div class="container">
<div class="card-deck mb-3 text-center">
{{#each pizze}}
<div class="card mb-4 shadow-sm">
<div class="card-header">
  <h4 class="my-0 font-weight-normal">{{dataValues.name}}</h4>
</div>
<div class="card-body">
  <h1 class="card-title pricing-card-title">€ {{dataValues.price}}</h1>
  <ul class="list-unstyled mt-3 mb-4">
    {{#each ingredients}}
    <li>{{dataValues.name}}</li>
    {{/each}}
  </ul>
  <button type="button" class="btn btn-lg btn-block btn-outline-primary">Ordina</button>
</div>
</div>
{{/each}}
</div>
